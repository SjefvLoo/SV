% Wafers are only moved to chuck Out when they are projected.

nu X(mp: Bool = false, pp: Bool = false).
[Projected]X(mp, true)
&& [SwapIdle]X(pp, true)
&& [RMoveFromTo(RobotThree, ChuckMeas, ChuckOut)](val(mp) && X(false, pp))