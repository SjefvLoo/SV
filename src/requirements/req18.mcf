% Wafers are only moved to chuck Out when they are projected.

nu X(mp: Bool = false, pp: Bool = false).
[Projected]X(mp, true)
&& [SwapIdle]X(pp, true)
&& [RMoveFromTo(RobotTwo, ChuckMeas, ChuckOut)](val(mp) && X(false, pp))
&& [!(Projected || SwapIdle || RMoveFromTo(RobotTwo, ChuckMeas, ChuckOut))]X(mp,pp)